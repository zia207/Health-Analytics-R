{
  "hash": "eb7e7ae1375c232453e55885e1045339",
  "result": {
    "engine": "knitr",
    "markdown": "![](Images/banner_health_analytics.png){fig-align=\"center\" width=\"100%\"}\n\n### Module 07: Spatial Epidemiology\n\n# Geostatistical Risk Analysis\n\n***\n**Learning objectives**\n- Understand indicator kriging (IK) as a non-parametric method for probabilistic risk mapping\n- Prepare BGS groundwater arsenic data, including detection-limit censoring\n- Transform continuous concentrations into binary exceedance indicators (WHO & Bangladesh thresholds)\n- Compute, model, and visualise indicator semivariograms with `gstat`\n- Validate IK predictions with k-fold cross-validation\n- Map the probability of exceeding regulatory arsenic limits on a national grid\n- Interpret IK surfaces for environmental health decision-making\n\n**Data:** `Data/bgs_geochemical.csv`, `Data/bd_grid.csv`, `Data/BD_Banladesh_BUTM.shp` (+ sidecar files)  \n**Context:** British Geological Survey (BGS) groundwater survey, 3,534 boreholes across 61 of 64 districts  \n**Thresholds:** 10 µg/L (WHO) and 50 µg/L (Bangladesh national standard)  \n**Estimated time:** 3 hours | **Level:** Advanced  \n**Libraries:** `sf`, `sp`, `gstat`, `ggplot2`, `dplyr`, `patchwork`\n\n## 1. Setup\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse)\nlibrary(sf)\nlibrary(sp)\nlibrary(gstat)\nlibrary(patchwork)\n\nset.seed(42)\nDATA <- \"Data\"\nTHRESHOLDS <- c(WHO = 10, Bangladesh = 50)  # µg/L (ppb)\n\ntheme_set(\n  theme_minimal(base_size = 11) +\n    theme(\n      plot.background = element_rect(fill = \"white\", colour = NA),\n      panel.background = element_rect(fill = \"white\", colour = NA),\n      panel.grid.minor = element_blank()\n    )\n)\n\ntheme_map <- function() {\n  theme_void(base_size = 11) +\n    theme(\n      plot.background = element_rect(fill = \"white\", colour = NA),\n      panel.background = element_rect(fill = \"white\", colour = NA),\n      plot.title = element_text(face = \"bold\", size = 11, hjust = 0.5),\n      plot.subtitle = element_text(size = 9, hjust = 0.5),\n      legend.position = \"right\"\n    )\n}\n\n# Original IK colour ramp (rasterVis / levelplot workflow)\nik_colours <- c(\"blue\", \"green\", \"yellow\", \"red\")\n\ncat(\"Setup complete\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSetup complete\n```\n\n\n:::\n:::\n\n\n## 2. Indicator Kriging for Environmental Health Risk\n\n**Indicator kriging (IK)** is a non-parametric geostatistical method. After an *indicator transformation* of a continuous variable at one or more pre-defined thresholds, ordinary kriging of the 0/1 indicators maps the **probability of exceeding** those thresholds. That probability surface is directly useful for regulatory and public-health decisions.\n\nIK can also reconstruct an entire cumulative distribution function (CDF) at unsampled locations. The mean of that CDF (the **E-type estimate**) is then an estimate of concentration after the upper and lower tails have been modelled [(Goovaerts, 2009)](https://www.ncbi.nlm.nih.gov/pubmed/20161335). CDF-based IK is attractive when the raw distribution is strongly skewed: log-transforms and other parametric estimators are easily dominated by extreme values.\n\nIn R, **`gstat`** implements indicator kriging of binary fields. There is still **no CRAN package that builds E-type estimates from IK CDFs**. That step is available in classical geostatistical software such as [GSLIB](http://www.gslib.com/) and [SGeMS](http://sgems.sourceforge.net/). **AUTO-IK** [(Goovaerts, 2009)](https://www.ncbi.nlm.nih.gov/pubmed/20161335) automates GSLIB routines for threshold selection, indicator variogram modelling, prediction of probability distributions on regular or irregular grids, and estimation of the mean and variance of those distributions.\n\n**Ordinary kriging** is the best linear unbiased predictor (BLUP) of a spatially distributed variable. At an unsampled location $\\mathbf{s}_0$:\n\n$$\n\\hat{Z}(\\mathbf{s}_0) = \\sum_{i=1}^n \\lambda_i Z(\\mathbf{s}_i)\n$$\n\nwhere $Z(\\mathbf{s}_i)$ are observed values and the weights $\\lambda_i$ minimise prediction variance subject to $\\sum_i \\lambda_i = 1$.\n\nIK applies the same estimator to a threshold indicator. For a cut-off $z_c$:\n\n$$\nI(\\mathbf{s}_i; z_c) =\n  \\begin{cases}\n    1 & \\text{if } Z(\\mathbf{s}_i) > z_c \\\\\n    0 & \\text{otherwise}\n  \\end{cases}\n$$\n\nKriging $I(\\mathbf{s}_i; z_c)$ estimates the conditional exceedance probability:\n\n$$\n\\mathbb{P}[Z(\\mathbf{s}_0) > z_c \\mid \\text{data}] \\approx \\hat{I}(\\mathbf{s}_0; z_c) = \\sum_{i=1}^n \\lambda_i I(\\mathbf{s}_i; z_c)\n$$\n\n| Application | Why IK helps |\n|-------------|-------------|\n| **Regulatory compliance** | Map P(As > 10 µg/L) against the WHO guideline |\n| **National standards** | Map P(As > 50 µg/L) against the Bangladesh drinking-water standard |\n| **Skewed distributions** | Arsenic is strongly right-skewed; log-transforms distort tail behaviour |\n| **Censored data** | Detection-limit samples can be handled *before* indicator coding |\n\nThis notebook follows the classic BGS Bangladesh arsenic workflow:\n\n1. Convert arsenic concentrations to indicators at 10 and 50 µg/L\n2. Compute and model indicator semivariograms\n3. Predict exceedance probabilities by indicator kriging\n4. Map those probabilities on a national prediction grid\n\n## 3. Load Data\n\nWe use the British Geological Survey [groundwater arsenic survey](http://www.bgs.ac.uk/research/groundwater/health/arsenic/Bangladesh/data.html): hydrochemical data from **3,534 boreholes** in **61 of 64** districts. Prediction locations are the national 5 km grid (`bd_grid.csv`); the country outline is the BUTM shapefile.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndf   <- read_csv(file.path(DATA, \"bgs_geochemical.csv\"), show_col_types = FALSE)\n```\n\n::: {.cell-output .cell-output-stderr}\n\n```\nWarning: One or more parsing issues, call `problems()` on your data frame for details,\ne.g.:\n  dat <- vroom(...)\n  problems(dat)\n```\n\n\n:::\n\n```{.r .cell-code}\ngrid <- read_csv(file.path(DATA, \"bd_grid.csv\"), show_col_types = FALSE)\nbd   <- st_read(file.path(DATA, \"BD_Banladesh_BUTM.shp\"), quiet = TRUE)\n\ncat(sprintf(\"BGS samples : %s boreholes\\n\", format(nrow(df), big.mark = \",\")))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBGS samples : 3,534 boreholes\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(sprintf(\"Prediction grid: %s cells\\n\", format(nrow(grid), big.mark = \",\")))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nPrediction grid: 5,339 cells\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(sprintf(\"Bangladesh boundary CRS: BUTM (Bangladesh Universal Transverse Mercator)\\n\"))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nBangladesh boundary CRS: BUTM (Bangladesh Universal Transverse Mercator)\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(sprintf(\"Polygons in shapefile: %d\\n\", nrow(bd)))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nPolygons in shapefile: 369\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(head(df %>% select(SAMPLE_ID, LONG_DEG, LAT_DEG, DISTRICT, WELL_TYPE, As), 3))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 3 × 6\n  SAMPLE_ID LONG_DEG LAT_DEG DISTRICT  WELL_TYPE    As\n  <chr>        <dbl>   <dbl> <chr>     <chr>     <dbl>\n1 S98_02701     90.1    22.4 Jhalakati DTW         0.5\n2 S98_02715     89.4    23.9 Pabna     STW         0.5\n3 S98_02979     91.6    24.6 Habiganj  STW         0.5\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(summary(df$As))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. \n   0.25    0.25    3.90   55.17   49.98 1660.00 \n```\n\n\n:::\n:::\n\n\n### 3.1 Handle detection limits\n\nAbout **27.7%** of sampled wells had arsenic below the instrumental detection limit of **0.5 µg/L** (hydride generation–atomic fluorescence spectrometry) and **2.5%** below **6.0 µg/L** (hydride generation–ICP-AES). Following the BGS protocol, values below a detection limit are assigned **half the equipment detection limit** (0.25 or 3.0 µg/L). Many records in this file are already stored at those half-DL values; remaining readings reported exactly at 0.5 or 6.0 µg/L are converted here.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nas_raw <- suppressWarnings(as.numeric(as.character(df$As)))\n\n# Half-DL substitution: HG-AFS (0.5 → 0.25) and HG-ICP-AES (6.0 → 3.0)\ndf$As_ppb <- case_when(\n  is.na(as_raw)              ~ NA_real_,\n  as_raw == 0.5              ~ 0.25,\n  as_raw == 6.0              ~ 3.0,\n  TRUE                       ~ as_raw\n)\n\nn_dl_afs <- sum(as_raw == 0.5, na.rm = TRUE)\nn_dl_icp <- sum(as_raw == 6.0, na.rm = TRUE)\nn_half_afs <- sum(df$As_ppb == 0.25, na.rm = TRUE)\nn_half_icp <- sum(df$As_ppb == 3.0, na.rm = TRUE)\n\ncat(sprintf(\"HG-AFS DL readings (0.5 → 0.25 µg/L): %s\\n\", format(n_dl_afs, big.mark = \",\")))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nHG-AFS DL readings (0.5 → 0.25 µg/L): 33\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(sprintf(\"HG-ICP-AES DL readings (6.0 → 3.0 µg/L): %s\\n\", format(n_dl_icp, big.mark = \",\")))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nHG-ICP-AES DL readings (6.0 → 3.0 µg/L): 6\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(sprintf(\"Values at half-DL 0.25 µg/L after substitution: %s (%.1f%%)\\n\",\n            format(n_half_afs, big.mark = \",\"), 100 * n_half_afs / nrow(df)))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nValues at half-DL 0.25 µg/L after substitution: 1,027 (29.1%)\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(sprintf(\"Values at half-DL 3.0 µg/L after substitution: %s\\n\",\n            format(n_half_icp, big.mark = \",\")))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nValues at half-DL 3.0 µg/L after substitution: 15\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(summary(df$As_ppb))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. \n   0.25    0.25    3.80   55.17   49.98 1660.00 \n```\n\n\n:::\n:::\n\n\nCensoring at 0.25 or 3.0 µg/L does not change WHO (10) or Bangladesh (50) indicators, but it is the correct protocol before any later E-type / multi-threshold analysis.\n\n## 4. Exploratory Data Analysis\n\nArsenic is strongly right-skewed, which is exactly the setting where indicator coding is more robust than kriging the raw (or log) concentrations.\n\n\n::: {.cell}\n\n```{.r .cell-code}\np_hist <- ggplot(df, aes(As_ppb)) +\n  geom_histogram(bins = 20, fill = \"steelblue\", colour = \"white\") +\n  geom_vline(xintercept = unname(THRESHOLDS), colour = \"#DC2626\", linetype = \"dashed\") +\n  labs(x = \"As (µg/L)\", y = \"Count\", title = \"Histogram of groundwater As\")\n\np_qq <- ggplot(df, aes(sample = As_ppb)) +\n  stat_qq(colour = \"steelblue\", size = 0.8, alpha = 0.6) +\n  stat_qq_line(colour = \"steelblue\", linewidth = 0.8) +\n  labs(x = \"Theoretical quantiles\", y = \"Sample quantiles\",\n       title = \"Normal Q–Q plot of As\")\n\nprint(p_hist | p_qq)\n```\n\n::: {.cell-output-display}\n![](MOD07_NB10_Geostatistical_Risk_Analysis_files/figure-html/unnamed-chunk-3-1.png){width=672}\n:::\n\n```{.r .cell-code}\nfor (nm in names(THRESHOLDS)) {\n  thr <- THRESHOLDS[[nm]]\n  pct <- 100 * mean(df$As_ppb > thr, na.rm = TRUE)\n  cat(sprintf(\"Samples exceeding %s standard (%g µg/L): %.1f%%\\n\", nm, thr, pct))\n}\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSamples exceeding WHO standard (10 µg/L): 42.1%\nSamples exceeding Bangladesh standard (50 µg/L): 24.9%\n```\n\n\n:::\n:::\n\n\n## 5. Project Sample Locations to BUTM\n\nSampling coordinates are stored as longitude/latitude (WGS 84). We reproject boreholes to **Bangladesh Universal Transverse Mercator (BUTM)** — the same projected CRS as the national boundary — so Euclidean distances in metres are meaningful for the semivariogram.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngdf <- st_as_sf(df, coords = c(\"LONG_DEG\", \"LAT_DEG\"), crs = 4326)\ngdf_proj <- st_transform(gdf, st_crs(bd))\nxy <- st_coordinates(gdf_proj)\n\nmf <- tibble(\n  x         = as.numeric(xy[, 1]),\n  y         = as.numeric(xy[, 2]),\n  As        = df$As_ppb,\n  DISTRICT  = df$DISTRICT,\n  WELL_TYPE = df$WELL_TYPE\n) %>% drop_na(As)\n\ncat(sprintf(\"Projected samples: %s\\n\", format(nrow(mf), big.mark = \",\")))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nProjected samples: 3,534\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(head(mf))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 6 × 5\n        x        y    As DISTRICT      WELL_TYPE\n    <dbl>    <dbl> <dbl> <chr>         <chr>    \n1 509574. 2474006.  0.25 Jhalakati     DTW      \n2 439963. 2647931.  0.25 Pabna         STW      \n3 662328. 2718502.  0.25 Habiganj      STW      \n4 619708. 2631583.  0.25 Brahamanbaria STW      \n5 454332. 2522667.  0.25 Khulna        DTW      \n6 438852. 2576967.  0.25 Magura        STW      \n```\n\n\n:::\n:::\n\n\n## 6. Indicator Transformation\n\nAn indicator for a continuous variable is **1 if the value exceeds a defined threshold**, 0 otherwise. We use the two regulatory cut-offs from the original BGS IK exercise:\n\n- **10 µg/L** — WHO drinking-water guideline\n- **50 µg/L** — Bangladesh national standard\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmf$ik_10 <- as.numeric(mf$As > THRESHOLDS[[\"WHO\"]])\nmf$ik_50 <- as.numeric(mf$As > THRESHOLDS[[\"Bangladesh\"]])\n\nik_sf <- st_as_sf(mf, coords = c(\"x\", \"y\"), crs = st_crs(bd), remove = FALSE)\n\ncat(sprintf(\"ik_10 (As > 10 µg/L): mean = %.3f (%d / %d samples above threshold)\\n\",\n            mean(mf$ik_10), sum(mf$ik_10), nrow(mf)))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nik_10 (As > 10 µg/L): mean = 0.421 (1487 / 3534 samples above threshold)\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(sprintf(\"ik_50 (As > 50 µg/L): mean = %.3f (%d / %d samples above threshold)\\n\",\n            mean(mf$ik_50), sum(mf$ik_50), nrow(mf)))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nik_50 (As > 50 µg/L): mean = 0.249 (881 / 3534 samples above threshold)\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(head(mf %>% select(x, y, As, ik_10, ik_50, DISTRICT)))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 6 × 6\n        x        y    As ik_10 ik_50 DISTRICT     \n    <dbl>    <dbl> <dbl> <dbl> <dbl> <chr>        \n1 509574. 2474006.  0.25     0     0 Jhalakati    \n2 439963. 2647931.  0.25     0     0 Pabna        \n3 662328. 2718502.  0.25     0     0 Habiganj     \n4 619708. 2631583.  0.25     0     0 Brahamanbaria\n5 454332. 2522667.  0.25     0     0 Khulna       \n6 438852. 2576967.  0.25     0     0 Magura       \n```\n\n\n:::\n:::\n\n\n## 7. Map Sample Data\n\n\n::: {.cell}\n\n```{.r .cell-code}\np_as <- ggplot() +\n  geom_sf(data = bd, fill = NA, colour = \"grey50\", linewidth = 0.3) +\n  geom_point(data = mf, aes(x, y, colour = As), size = 0.55, alpha = 0.85) +\n  scale_colour_gradientn(\n    name = \"As (µg/L)\",\n    colours = c(\"green\", \"orange\", \"red\"),\n    limits = c(0, 200), oob = scales::squish\n  ) +\n  labs(title = \"Groundwater As (µg/L)\", x = \"Easting (m)\", y = \"Northing (m)\") +\n  coord_sf()\n\nmap_ik <- function(col, title) {\n  ggplot() +\n    geom_sf(data = bd, fill = NA, colour = \"grey50\", linewidth = 0.3) +\n    geom_point(data = mf, aes(x, y, colour = factor(.data[[col]])),\n               size = 0.55, alpha = 0.85) +\n    scale_colour_manual(values = c(\"0\" = \"#16A34A\", \"1\" = \"#DC2626\"), guide = \"none\") +\n    labs(title = title, x = \"Easting (m)\", y = NULL) +\n    coord_sf()\n}\n\nprint(\n  p_as |\n    map_ik(\"ik_10\", \"As > 10 µg/L (WHO)\") |\n    map_ik(\"ik_50\", \"As > 50 µg/L (Bangladesh)\")\n)\n```\n\n::: {.cell-output-display}\n![](MOD07_NB10_Geostatistical_Risk_Analysis_files/figure-html/unnamed-chunk-6-1.png){width=1152}\n:::\n:::\n\n\nGreen = below the threshold (indicator 0); red = above (indicator 1).\n\n## 8. Indicator Semivariograms\n\nThe semivariogram quantifies how spatial similarity decays with distance $h$:\n\n$$\n\\gamma(h) = \\frac{1}{2N(h)} \\sum_{i,j;\\ |\\mathbf{s}_i-\\mathbf{s}_j| \\approx h} \\bigl[ Z(\\mathbf{s}_i) - Z(\\mathbf{s}_j) \\bigr]^2\n$$\n\nAn **indicator variogram** applies the same formula to $I(\\mathbf{s}; z_c)$. It describes the spatial continuity of high-risk vs low-risk zones rather than of concentration itself.\n\n`gstat` cannot krige coincident locations, so we drop zero-distance pairs with `zerodist()` (the original IK workflow). Initial exponential parameters are set by eye, then refined by weighted least squares — the same `vgm()` / `fit.variogram()` sequence as the source notebook.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Coincident boreholes: keep the first of each zerodist pair\nik_sp <- as(ik_sf, \"Spatial\")\nzd <- zerodist(ik_sp)\nn_coinc <- if (is.null(nrow(zd))) 0 else nrow(zd)\nif (n_coinc > 0) {\n  ik_sp <- ik_sp[-zd[, 1], ]\n}\nik_sf <- st_as_sf(ik_sp)\nmf <- st_drop_geometry(ik_sf)\n\ncat(sprintf(\"Unique sample locations after zerodist(): %s (dropped %d coincident points)\\n\",\n            format(nrow(ik_sf), big.mark = \",\"), n_coinc))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nUnique sample locations after zerodist(): 3,420 (dropped 115 coincident points)\n```\n\n\n:::\n\n```{.r .cell-code}\n# Experimental indicator variograms (gstat defaults: cutoff ≈ 1/3 bounding-box diagonal)\nv10 <- variogram(ik_10 ~ 1, data = ik_sf)\nv50 <- variogram(ik_50 ~ 1, data = ik_sf)\n\n# Initial exponential model set by eye, then WLS fit\nm10 <- vgm(psill = 0.15, model = \"Exp\", range = 40000, nugget = 0.05)\nm50 <- vgm(psill = 0.15, model = \"Exp\", range = 40000, nugget = 0.05)\nm_f_10 <- fit.variogram(v10, m10)\nm_f_50 <- fit.variogram(v50, m50)\n\ncat(\"Fitted indicator variogram  As > 10 µg/L:\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nFitted indicator variogram  As > 10 µg/L:\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(m_f_10)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  model     psill    range\n1   Nug 0.1346799     0.00\n2   Exp 0.1155823 46860.32\n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"\\nFitted indicator variogram  As > 50 µg/L:\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nFitted indicator variogram  As > 50 µg/L:\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(m_f_50)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  model     psill    range\n1   Nug 0.1005604     0.00\n2   Exp 0.1030162 56236.76\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot_variogram <- function(v_emp, v_fit, title) {\n  h_max <- max(v_emp$dist, na.rm = TRUE)\n  h <- seq(0, h_max, length.out = 300)\n  v_line <- variogramLine(v_fit, dist_vector = h)\n  ggplot() +\n    geom_point(data = v_emp, aes(dist, gamma), size = 2, shape = 19) +\n    geom_line(data = v_line, aes(dist, gamma), colour = \"black\", linewidth = 0.7) +\n    labs(x = \"Distance (m)\", y = \"Semivariance\", title = title)\n}\n\nprint(\n  plot_variogram(v10, m_f_10, \"Indicator variogram\\nAs > 10 µg/L\") |\n    plot_variogram(v50, m_f_50, \"Indicator variogram\\nAs > 50 µg/L\")\n)\n```\n\n::: {.cell-output-display}\n![](MOD07_NB10_Geostatistical_Risk_Analysis_files/figure-html/unnamed-chunk-8-1.png){width=672}\n:::\n:::\n\n\n## 9. Cross-Validation\n\nWe run **5-fold cross-validation** (`gstat::krige.cv(..., nfold = 5)`), the same procedure as in the original IK notebook (described there as leave-one-out in spirit: hold out a fold, predict P(indicator = 1) from the remaining points, compare with the observed 0/1). Predicted probabilities are then clipped to $[0, 1]$ — kriging of indicators is not guaranteed to stay inside that interval.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncv_10 <- krige.cv(ik_10 ~ 1, locations = ik_sf, model = m_f_10, nfold = 5, verbose = FALSE)\ncv_50 <- krige.cv(ik_50 ~ 1, locations = ik_sf, model = m_f_50, nfold = 5, verbose = FALSE)\n\ncv_10$var1.pred <- pmin(1, pmax(0, cv_10$var1.pred))\ncv_50$var1.pred <- pmin(1, pmax(0, cv_50$var1.pred))\n\ncat(\"CV  As > 10 µg/L:\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nCV  As > 10 µg/L:\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(summary(cv_10))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   var1.pred         var1.var         observed         residual         \n Min.   :0.0000   Min.   :0.1449   Min.   :0.0000   Min.   :-0.9950299  \n 1st Qu.:0.1280   1st Qu.:0.1550   1st Qu.:0.0000   1st Qu.:-0.2474077  \n Median :0.3954   Median :0.1572   Median :0.0000   Median :-0.0215171  \n Mean   :0.4227   Mean   :0.1578   Mean   :0.4216   Mean   :-0.0009756  \n 3rd Qu.:0.6876   3rd Qu.:0.1597   3rd Qu.:1.0000   3rd Qu.: 0.2435354  \n Max.   :1.0000   Max.   :0.2112   Max.   :1.0000   Max.   : 1.0025479  \n     zscore               fold               geometry   \n Min.   :-2.508652   Min.   :1.00   POINT        :3420  \n 1st Qu.:-0.620463   1st Qu.:2.00   epsg:NA      :   0  \n Median :-0.054180   Median :3.00   +proj=tmer...:   0  \n Mean   :-0.002278   Mean   :3.01                       \n 3rd Qu.: 0.615455   3rd Qu.:4.00                       \n Max.   : 2.537233   Max.   :5.00                       \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"\\nCV  As > 50 µg/L:\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nCV  As > 50 µg/L:\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(summary(cv_50))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   var1.pred          var1.var         observed        residual         \n Min.   :0.00000   Min.   :0.1083   Min.   :0.000   Min.   :-0.9928603  \n 1st Qu.:0.02507   1st Qu.:0.1157   1st Qu.:0.000   1st Qu.:-0.1588994  \n Median :0.14437   Median :0.1173   Median :0.000   Median :-0.0255594  \n Mean   :0.25325   Mean   :0.1178   Mean   :0.252   Mean   :-0.0008994  \n 3rd Qu.:0.40135   3rd Qu.:0.1193   3rd Qu.:1.000   3rd Qu.: 0.0109059  \n Max.   :1.00000   Max.   :0.1621   Max.   :1.000   Max.   : 1.0070914  \n     zscore              fold                geometry   \n Min.   :-2.90646   Min.   :1.000   POINT        :3420  \n 1st Qu.:-0.46499   1st Qu.:2.000   epsg:NA      :   0  \n Median :-0.07445   Median :3.000   +proj=tmer...:   0  \n Mean   :-0.00247   Mean   :2.982                       \n 3rd Qu.: 0.03156   3rd Qu.:4.000                       \n Max.   : 2.98161   Max.   :5.000                       \n```\n\n\n:::\n\n```{.r .cell-code}\nreport_cv <- function(name, pred, obs) {\n  brier <- mean((pred - obs)^2, na.rm = TRUE)\n  cat(sprintf(\"As > %s: Brier score = %.4f | pred range [%.3f, %.3f]\\n\",\n              name, brier, min(pred, na.rm = TRUE), max(pred, na.rm = TRUE)))\n}\nreport_cv(\"10 µg/L\", cv_10$var1.pred, cv_10$observed)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nAs > 10 µg/L: Brier score = 0.1454 | pred range [0.000, 1.000]\n```\n\n\n:::\n\n```{.r .cell-code}\nreport_cv(\"50 µg/L\", cv_50$var1.pred, cv_50$observed)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nAs > 50 µg/L: Brier score = 0.1095 | pred range [0.000, 1.000]\n```\n\n\n:::\n:::\n\n\nPost-plots: symbol **size is proportional to predicted probability**; **green = observed indicator FALSE** (below threshold), **red = TRUE** (above).\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncv_map <- function(cv, title) {\n  xy_cv <- st_coordinates(st_as_sf(cv))\n  plot_df <- tibble(\n    x     = xy_cv[, 1],\n    y     = xy_cv[, 2],\n    obs   = as.integer(cv$observed > 0.5),\n    pred  = cv$var1.pred\n  )\n  ggplot() +\n    geom_sf(data = bd, fill = NA, colour = \"grey50\", linewidth = 0.3) +\n    geom_point(\n      data = plot_df,\n      aes(x, y, colour = factor(obs), size = 0.4 + 1.6 * pred),\n      alpha = 0.65, stroke = 0\n    ) +\n    scale_colour_manual(values = c(\"0\" = \"#16A34A\", \"1\" = \"#DC2626\"), guide = \"none\") +\n    scale_size_identity() +\n    labs(\n      title = title, x = \"Easting (m)\", y = \"Northing (m)\",\n      subtitle = \"Actual indicator: green / red = FALSE / TRUE; size ∝ predicted probability\"\n    ) +\n    coord_sf()\n}\n\nprint(\n  cv_map(cv_10, \"Probability of TRUE indicator (10 µg/L)\") |\n    cv_map(cv_50, \"Probability of TRUE indicator (50 µg/L)\")\n)\n```\n\n::: {.cell-output-display}\n![](MOD07_NB10_Geostatistical_Risk_Analysis_files/figure-html/unnamed-chunk-10-1.png){width=1152}\n:::\n:::\n\n\n## 10. Indicator Kriging on the Prediction Grid\n\nOrdinary IK at each grid cell, using the **50 nearest neighbours** (`nmax = 50`) and the fitted exponential models — matching the original `gstat::krige()` call.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngrid_sf <- st_as_sf(grid, coords = c(\"x\", \"y\"), crs = st_crs(bd), remove = FALSE)\n\nik_grid_10 <- krige(ik_10 ~ 1, nmax = 50,\n                    locations = ik_sf,\n                    newdata = grid_sf,\n                    model = m_f_10,\n                    debug.level = 0)\nik_grid_50 <- krige(ik_50 ~ 1, nmax = 50,\n                    locations = ik_sf,\n                    newdata = grid_sf,\n                    model = m_f_50,\n                    debug.level = 0)\n\ncat(\"Raw IK  As > 10 µg/L:\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nRaw IK  As > 10 µg/L:\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(summary(ik_grid_10$var1.pred))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     Min.   1st Qu.    Median      Mean   3rd Qu.      Max. \n-0.006402  0.097765  0.368668  0.407622  0.684044  1.000148 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"\\nRaw IK  As > 50 µg/L:\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nRaw IK  As > 50 µg/L:\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(summary(ik_grid_50$var1.pred))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     Min.   1st Qu.    Median      Mean   3rd Qu.      Max. \n-0.005204  0.014550  0.129556  0.237453  0.378431  1.000370 \n```\n\n\n:::\n:::\n\n\nClip predicted probabilities to $[0, 1]$:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nik_grid_10$var1.pred <- pmin(1, pmax(0, ik_grid_10$var1.pred))\nik_grid_50$var1.pred <- pmin(1, pmax(0, ik_grid_50$var1.pred))\n\ngrid_out <- grid %>%\n  mutate(\n    p_as_gt_10 = ik_grid_10$var1.pred,\n    p_as_gt_50 = ik_grid_50$var1.pred\n  )\n\ncat(\"Clipped P(As > 10 µg/L):\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nClipped P(As > 10 µg/L):\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(summary(grid_out$p_as_gt_10))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. \n0.00000 0.09777 0.36867 0.40764 0.68404 1.00000 \n```\n\n\n:::\n\n```{.r .cell-code}\ncat(\"\\nClipped P(As > 50 µg/L):\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nClipped P(As > 50 µg/L):\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(summary(grid_out$p_as_gt_50))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. \n0.00000 0.01455 0.12956 0.23748 0.37843 1.00000 \n```\n\n\n:::\n:::\n\n\n## 11. Probability Maps\n\nThe original workflow converted the kriging output to a raster (`rasterFromXYZ`) and plotted it with `rasterVis::levelplot()` using a blue–green–yellow–red ramp. We build the same surface with `geom_tile` on the 5 km grid.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprobability_surface <- function(grid_df, value_col, title) {\n  ggplot() +\n    geom_tile(\n      data = grid_df,\n      aes(x, y, fill = .data[[value_col]]),\n      width = 5000, height = 5000\n    ) +\n    geom_sf(data = bd, fill = NA, colour = \"grey30\", linewidth = 0.25) +\n    scale_fill_gradientn(\n      name = \"Probability\",\n      colours = ik_colours,\n      limits = c(0, 1)\n    ) +\n    labs(title = title, x = \"Easting (m)\", y = \"Northing (m)\") +\n    coord_sf() +\n    theme_map() +\n    theme(\n      axis.title = element_text(size = 9),\n      axis.text = element_text(size = 8),\n      legend.position = \"right\"\n    )\n}\n\nprint(\n  probability_surface(grid_out, \"p_as_gt_10\", \"Probability As > 10 µg/L\") |\n    probability_surface(grid_out, \"p_as_gt_50\", \"Probability As > 50 µg/L\")\n)\n```\n\n::: {.cell-output-display}\n![](MOD07_NB10_Geostatistical_Risk_Analysis_files/figure-html/unnamed-chunk-13-1.png){width=960}\n:::\n:::\n\n\n## 12. District-Level Risk Summary\n\nThe original IK notebook stopped at the national probability maps. For health-policy use we summarise, by BGS **DISTRICT**, the fraction of sampled wells exceeding each threshold and the mean IK probability at those well locations (nearest grid cell).\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngrid_pts <- st_as_sf(grid_out, coords = c(\"x\", \"y\"), crs = st_crs(bd))\nnn <- st_nearest_feature(ik_sf, grid_pts)\nmf_out <- mf %>%\n  mutate(\n    p_as_gt_10 = grid_out$p_as_gt_10[nn],\n    p_as_gt_50 = grid_out$p_as_gt_50[nn]\n  )\n\ndistrict_risk <- mf_out %>%\n  group_by(DISTRICT) %>%\n  summarise(\n    n_wells      = n(),\n    frac_gt_10   = mean(ik_10),\n    frac_gt_50   = mean(ik_50),\n    mean_p_gt_10 = mean(p_as_gt_10, na.rm = TRUE),\n    mean_p_gt_50 = mean(p_as_gt_50, na.rm = TRUE),\n    median_as    = median(As),\n    .groups = \"drop\"\n  ) %>%\n  arrange(desc(frac_gt_10))\n\ncat(\"Top 10 districts by sampled exceedance of WHO guideline (10 µg/L):\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nTop 10 districts by sampled exceedance of WHO guideline (10 µg/L):\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(district_risk %>% head(10) %>% mutate(across(where(is.numeric), ~ round(.x, 3))))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 10 × 7\n   DISTRICT   n_wells frac_gt_10 frac_gt_50 mean_p_gt_10 mean_p_gt_50 median_as\n   <chr>        <dbl>      <dbl>      <dbl>        <dbl>        <dbl>     <dbl>\n 1 Chandpur        53      0.981      0.981        0.966        0.953     407  \n 2 Meherpur        13      0.923      0.615        0.851        0.568      62.5\n 3 Munshiganj      45      0.911      0.844        0.861        0.804     187  \n 4 Noakhali        45      0.889      0.756        0.862        0.722     124  \n 5 Sunamganj       68      0.868      0.412        0.846        0.404      44.4\n 6 Gopalganj       40      0.85       0.8          0.849        0.795     163  \n 7 Bagerhat        59      0.831      0.61         0.766        0.559     103  \n 8 Chuadanga       32      0.812      0.438        0.8          0.454      46.0\n 9 Satkhira        56      0.804      0.679        0.79         0.644     106  \n10 Lakshmipur      31      0.774      0.581        0.754        0.609      82.6\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntop <- district_risk %>% slice_head(n = 12) %>%\n  mutate(DISTRICT = fct_reorder(DISTRICT, frac_gt_10))\n\np_d1 <- ggplot(top, aes(frac_gt_10, DISTRICT)) +\n  geom_col(fill = \"coral\", colour = \"white\") +\n  coord_cartesian(xlim = c(0, 1)) +\n  labs(x = \"Fraction of wells with As > 10 µg/L\",\n       y = NULL, title = \"Observed exceedance by district (top 12)\")\n\np_d2 <- ggplot(top, aes(mean_p_gt_10, DISTRICT)) +\n  geom_col(fill = \"steelblue\", colour = \"white\") +\n  labs(x = \"Mean P(As > 10 µg/L) at wells\",\n       y = NULL, title = \"IK probability at well locations (top 12)\")\n\nprint(p_d1 | p_d2)\n```\n\n::: {.cell-output-display}\n![](MOD07_NB10_Geostatistical_Risk_Analysis_files/figure-html/unnamed-chunk-15-1.png){width=672}\n:::\n:::\n\n\n## 13. Interpretation & Health Policy Context\n\n**Key findings from indicator kriging:**\n\n- IK maps *where* groundwater is likely unsafe, not only where boreholes were drilled — essential because sample density is uneven.\n- The 10 µg/L (WHO) surface is more extensive than the 50 µg/L (Bangladesh) surface, as expected from the stricter guideline.\n- Ordinary kriging of indicators can produce probabilities slightly outside $[0, 1]$; clipping after prediction is standard.\n- Cross-validation Brier scores summarise calibration; lower is better.\n- `gstat` delivers the probability maps; E-type concentration estimates would require multiple thresholds and CDF reconstruction (AUTO-IK / GSLIB / SGeMS).\n\n**Limitations:**\n\n- IK assumes stationarity of the indicator semivariogram.\n- Half-DL substitution (0.25 / 3.0 µg/L) does not affect the 10 and 50 µg/L indicators, but it would matter for a full IK CDF.\n- A single exponential model is a simplification; nested structures or anisotropy can be added if the empirical variogram warrants it.\n\n**References:**\n\n- Goovaerts P. (2009). AUTO-IK: a geostatistical program for indicator kriging. *Environ Sci Technol*.\n- BGS Bangladesh arsenic survey: [bgs.ac.uk groundwater health data](http://www.bgs.ac.uk/research/groundwater/health/arsenic/Bangladesh/data.html)\n\n## 14. Knowledge Check\n\n1. Why is indicator kriging preferred over ordinary kriging of log-arsenic when the goal is a *regulatory exceedance* map?\n2. What does a predicted probability of 0.7 at an unmonitored grid cell mean for a household relying on a local tube well?\n3. Why does `gstat` require `zerodist()` (or equivalent) before kriging, and what would happen if coincident points were left in?\n4. The original notebook used an exponential model with `vgm(0.15, \"Exp\", 40000, 0.05)` as a starting guess. How would you check whether that model is adequate for the 50 µg/L indicator?\n5. If the government wanted to prioritise well testing, how would you use the P(As > 50 µg/L) map together with the district table to rank districts?\n6. Extend the analysis: add a third threshold (e.g. 200 µg/L) and compare the spatial pattern of *severe* poisoning risk. What extra step would AUTO-IK take that this notebook does not?\n\n***\n**Next → Module 08: Reproducible Research & Deployment**\n",
    "supporting": [
      "MOD07_NB10_Geostatistical_Risk_Analysis_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}